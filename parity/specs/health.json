{
  "name": "health-envelope",
  "contract": "C-01",
  "request": { "method": "GET", "path": "/health" },
  "expect": {
    "status": 200,
    "json": { "status": "success", "data": { "status": "ok" } },
    "jsonContains": ["data.checks.database"],
    "tolerances": ["timestamps", "request ids"]
  }
}
