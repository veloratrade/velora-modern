// Web-local mirror of the frozen locale contract (ADR-009).
// Source: packages/contracts/src/locale.ts — copied verbatim so the web can build
// without requiring Next to transpile the NodeNext contracts package.
// Changes to the contract must be made in packages/contracts and copied here.
export * from "./locale";
